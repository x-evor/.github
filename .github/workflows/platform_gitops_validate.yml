name: Platform GitOps Validate

on:
  workflow_dispatch:
  push:
    paths:
      - "docs/plans/2026-04-02-single-node-k3s-gitops-platform.md"
      - "docs/operations-governance/release-checklist.md"
      - ".github/workflows/platform_gitops_validate.yml"
      - ".github/workflows/platform_k3s_bootstrap.yml"
      - "scripts/github-actions/run-platform-ansible.sh"
      - "ansible/inventory.ini"
      - "ansible/playbooks/harden_ssh_root_key_only.yml"
      - "ansible/roles/harden_ssh_root_key_only/**"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout control repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Checkout gitops repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          repository: ${{ github.repository_owner }}/gitops
          token: ${{ secrets.WORKSPACE_REPO_TOKEN || github.token }}
          path: gitops

      - name: Install lint tools
        shell: bash
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install yamllint
          curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          curl -sSL https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv5.4.2/kustomize_v5.4.2_linux_amd64.tar.gz | tar -xz
          sudo mv kustomize /usr/local/bin/kustomize

      - name: Validate workflow and ansible yaml
        shell: bash
        run: |
          yamllint .github/workflows/platform_k3s_bootstrap.yml .github/workflows/platform_gitops_validate.yml
          yamllint ansible/playbooks/deploy_k3s_core_migration.yml

      - name: Validate playbooks syntax
        shell: bash
        run: |
          ANSIBLE_CONFIG=ansible/ansible.cfg ansible-playbook -i ansible/inventory.ini ansible/playbooks/harden_ssh_root_key_only.yml --syntax-check

      - name: Verify helper syntax
        shell: bash
        run: bash -n scripts/github-actions/run-platform-ansible.sh

      - name: Validate gitops renders
        shell: bash
        run: |
          cd gitops
          kustomize build infra/clusters/prod >/tmp/prod.yaml
          kustomize build infra/clusters/pre >/tmp/pre.yaml
          helm template console-prod helm/app-service -f infra/apps/core/console/base/values.yaml -f infra/apps/core/console/prod/values.yaml >/tmp/console.yaml
          helm template accounts-pre helm/app-service -f infra/apps/core/accounts/base/values.yaml -f infra/apps/core/accounts/pre/values.yaml >/tmp/accounts.yaml
