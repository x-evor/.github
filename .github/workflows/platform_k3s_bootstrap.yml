name: Platform k3s Bootstrap

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Bootstrap mode"
        required: true
        default: "dry-run"
        type: choice
        options:
          - dry-run
          - apply
      target_host:
        description: "Inventory host alias"
        required: true
        default: "jp-xhttp-contabo.svc.plus"
        type: string
      ssh_host:
        description: "Optional override SSH host or IP; leave blank to use inventory ansible_host"
        required: false
        default: ""
        type: string
      ssh_user:
        description: "SSH username"
        required: true
        default: "root"
        type: string
      ssh_port:
        description: "SSH port"
        required: true
        default: "22"
        type: string
      ssh_known_hosts:
        description: "Known hosts content"
        required: false
        default: ""
        type: string
  push:
    paths:
      - "docs/plans/2026-04-02-single-node-k3s-gitops-platform.md"
      - "docs/operations-governance/release-checklist.md"
      - ".github/workflows/platform_k3s_bootstrap.yml"
      - "ansible/inventory.ini"
      - "ansible/playbooks/harden_ssh_root_key_only.yml"
      - "ansible/roles/harden_ssh_root_key_only/**"

permissions:
  contents: read

concurrency:
  group: platform-k3s-bootstrap-${{ github.event_name == 'workflow_dispatch' && inputs.target_host || github.ref_name }}
  cancel-in-progress: false

jobs:
  preflight:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout control repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Checkout gitops repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          repository: x-evor/gitops
          token: ${{ secrets.WORKSPACE_REPO_TOKEN || github.token }}
          path: gitops

      - name: Checkout playbooks repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          repository: x-evor/playbooks
          token: ${{ secrets.WORKSPACE_REPO_TOKEN || github.token }}
          path: playbooks

      - name: Checkout charts repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          repository: x-evor/artifacts
          token: ${{ secrets.WORKSPACE_REPO_TOKEN || github.token }}
          path: artifacts

      - name: Install lint tools
        shell: bash
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install yamllint
          curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          curl -sSL https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv5.4.2/kustomize_v5.4.2_linux_amd64.tar.gz | tar -xz
          sudo mv kustomize /usr/local/bin/kustomize

      - name: Validate workflow and ansible yaml
        shell: bash
        run: |
          yamllint -d '{extends: default, rules: {document-start: disable, truthy: disable, line-length: {max: 200}}}' .github/workflows/platform_k3s_bootstrap.yml
          yamllint -d '{extends: default, rules: {document-start: disable, truthy: disable, line-length: {max: 200}}}' ansible/playbooks/deploy_k3s_core_migration.yml

      - name: Validate playbooks syntax
        shell: bash
        run: |
          ANSIBLE_CONFIG=ansible/ansible.cfg ansible-playbook -i ansible/inventory.ini ansible/playbooks/harden_ssh_root_key_only.yml --syntax-check

      - name: Verify helper syntax
        shell: bash
        run: python3 -m py_compile scripts/github-actions/private-key-payload.py

      - name: Validate gitops renders
        shell: bash
        run: |
          gitops_root="${GITHUB_WORKSPACE}/gitops"
          kubectl kustomize "${gitops_root}/environments/clusters/prod" >/tmp/gitops-prod.yaml
          kubectl kustomize "${gitops_root}/environments/clusters/pre" >/tmp/gitops-pre.yaml

  bootstrap:
    needs: preflight
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout control repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Checkout playbooks repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          repository: x-evor/playbooks
          token: ${{ secrets.WORKSPACE_REPO_TOKEN || github.token }}
          path: playbooks

      - name: Install Ansible runtime
        uses: ./.github/actions/install-ansible-runtime

      - name: Prepare runner SSH key
        env:
          SINGLE_NODE_VPS_SSH_PRIVATE_KEY: ${{ secrets.SINGLE_NODE_VPS_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          python3 scripts/github-actions/private-key-payload.py normalize > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keygen -y -f ~/.ssh/id_rsa >/dev/null

      - name: Bootstrap k3s platform
        env:
          # Stage 1: install and configure k3s.
          ANSIBLE_OVERRIDE_SSH_HOST: ${{ inputs.ssh_host }}
          ANSIBLE_OVERRIDE_SSH_USER: ${{ inputs.ssh_user }}
          ANSIBLE_OVERRIDE_SSH_PORT: ${{ inputs.ssh_port }}
          ANSIBLE_OVERRIDE_SSH_KNOWN_HOSTS: ${{ inputs.ssh_known_hosts }}
          SINGLE_NODE_VPS_SSH_PRIVATE_KEY: ${{ secrets.SINGLE_NODE_VPS_SSH_PRIVATE_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN || secrets.CLOUDFLARE_DNS_API_TOKEN }}
          CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          VAULT_ROOT_TOKEN: ${{ secrets.VAULT_ROOT_TOKEN }}

          # Stage 2: integrate external Vault bootstrap inputs.
          VAULT_URL: ${{ vars.VAULT_URL || '' }}
          VAULT_NAMESPACE: ${{ vars.VAULT_NAMESPACE || '' }}
          VAULT_TOKEN: ${{ secrets.VAULT_TOKEN || '' }}

          # Stage 3: install and configure FluxCD bootstrap.
          GITOPS_REPO: ${{ vars.GITOPS_REPO || 'https://github.com/x-evor/gitops.git' }}
          GITOPS_AUTH_MODE: ${{ secrets.GITOPS_AUTH_MODE || vars.GITOPS_AUTH_MODE || 'public' }}
          GITOPS_FLUX_DEPLOY_KEY: ${{ secrets.GITOPS_FLUX_DEPLOY_KEY }}
          GITOPS_FLUX_DEPLOY_KEY_PUB: ${{ secrets.GITOPS_FLUX_DEPLOY_KEY_PUB }}
          K3S_PLATFORM_FLUX_ROOT_PATH: ./environments/clusters/prod
          BOOTSTRAP_MODE: ${{ inputs.mode }}
        shell: bash
        run: |
          export ANSIBLE_CONFIG="${{ github.workspace }}/ansible/ansible.cfg"
          export ANSIBLE_SSH_ARGS='-o ControlMaster=no -o ControlPersist=no'

          target_host="${{ inputs.target_host }}"
          ssh_host_arg=()
          if [[ -n "${{ inputs.ssh_host }}" ]]; then
            ssh_host_arg=(-e "ansible_host=${{ inputs.ssh_host }}")
          fi

          ansible_args=(
            ansible-playbook
            -i "${{ github.workspace }}/ansible/inventory.ini"
            -D
            -l "${target_host}"
            -e "ansible_user=${{ inputs.ssh_user }}"
            -e "ansible_port=${{ inputs.ssh_port }}"
          )
          if [[ ${#ssh_host_arg[@]} -gt 0 ]]; then
            ansible_args+=("${ssh_host_arg[@]}")
          fi
          if [[ "${BOOTSTRAP_MODE}" == "dry-run" ]]; then
            ansible_args+=(-C)
          fi
          ansible_args+=(
            "${{ github.workspace }}/playbooks/k3s_platform_bootstrap_with_gitops.yml"
          )
          "${ansible_args[@]}"

      - name: Run k3s platform addon
        env:
          ANSIBLE_OVERRIDE_SSH_HOST: ${{ inputs.ssh_host }}
          ANSIBLE_OVERRIDE_SSH_USER: ${{ inputs.ssh_user }}
          ANSIBLE_OVERRIDE_SSH_PORT: ${{ inputs.ssh_port }}
          ANSIBLE_OVERRIDE_SSH_KNOWN_HOSTS: ${{ inputs.ssh_known_hosts }}
          SINGLE_NODE_VPS_SSH_PRIVATE_KEY: ${{ secrets.SINGLE_NODE_VPS_SSH_PRIVATE_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN || secrets.CLOUDFLARE_DNS_API_TOKEN }}
          CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          VAULT_ROOT_TOKEN: ${{ secrets.VAULT_ROOT_TOKEN }}
          VAULT_URL: ${{ vars.VAULT_URL || '' }}
          VAULT_NAMESPACE: ${{ vars.VAULT_NAMESPACE || '' }}
          VAULT_TOKEN: ${{ secrets.VAULT_TOKEN || '' }}
          GITOPS_REPO: ${{ vars.GITOPS_REPO || 'https://github.com/x-evor/gitops.git' }}
          GITOPS_AUTH_MODE: ${{ secrets.GITOPS_AUTH_MODE || vars.GITOPS_AUTH_MODE || 'public' }}
          GITOPS_FLUX_DEPLOY_KEY: ${{ secrets.GITOPS_FLUX_DEPLOY_KEY }}
          GITOPS_FLUX_DEPLOY_KEY_PUB: ${{ secrets.GITOPS_FLUX_DEPLOY_KEY_PUB }}
          K3S_PLATFORM_FLUX_ROOT_PATH: ./environments/clusters/prod
          BOOTSTRAP_MODE: ${{ inputs.mode }}
        shell: bash
        run: |
          export ANSIBLE_CONFIG="${{ github.workspace }}/ansible/ansible.cfg"
          export ANSIBLE_SSH_ARGS='-o ControlMaster=no -o ControlPersist=no'

          target_host="${{ inputs.target_host }}"
          ssh_host_arg=()
          if [[ -n "${{ inputs.ssh_host }}" ]]; then
            ssh_host_arg=(-e "ansible_host=${{ inputs.ssh_host }}")
          fi

          ansible_args=(
            ansible-playbook
            -i "${{ github.workspace }}/ansible/inventory.ini"
            -D
            -l "${target_host}"
            -e "ansible_user=${{ inputs.ssh_user }}"
            -e "ansible_port=${{ inputs.ssh_port }}"
          )
          if [[ ${#ssh_host_arg[@]} -gt 0 ]]; then
            ansible_args+=("${ssh_host_arg[@]}")
          fi
          if [[ "${BOOTSTRAP_MODE}" == "dry-run" ]]; then
            ansible_args+=(-C)
          fi
          ansible_args+=(
            "${{ github.workspace }}/playbooks/k3s_platform_addon.yml"
          )
          "${ansible_args[@]}"

  validate:
    needs: bootstrap
    if: github.event_name == 'workflow_dispatch' && inputs.mode == 'apply'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        check:
          - name: "nodes"
            script: |
              kubectl get nodes -o wide
          - name: "namespaces"
            script: |
              kubectl get ns
          - name: "node-ready"
            script: |
              kubectl wait --for=condition=Ready node --all --timeout=10m
          - name: "flux-pods"
            script: |
              kubectl -n flux-system wait --for=condition=Ready pod --all --timeout=10m
          - name: "reconcile-source"
            script: |
              flux reconcile source git platform-config -n flux-system --timeout=5m
          - name: "reconcile-kustomization"
            script: |
              flux reconcile kustomization platform-root -n flux-system --with-source --timeout=10m
          - name: "flux-sources"
            script: |
              flux get sources git -A
          - name: "flux-kustomizations"
            script: |
              flux get kustomizations -A
          - name: "resources"
            script: |
              kubectl get gitrepositories,kustomizations,helmreleases -A
          - name: "pods"
            script: |
              kubectl get pods -A
    steps:
      - name: Checkout control repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Checkout playbooks repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          repository: x-evor/playbooks
          token: ${{ secrets.WORKSPACE_REPO_TOKEN || github.token }}
          path: playbooks

      - name: Install Ansible runtime
        uses: ./.github/actions/install-ansible-runtime

      - name: Prepare runner SSH key
        env:
          SINGLE_NODE_VPS_SSH_PRIVATE_KEY: ${{ secrets.SINGLE_NODE_VPS_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          python3 scripts/github-actions/private-key-payload.py normalize > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keygen -y -f ~/.ssh/id_rsa >/dev/null

      - name: Resolve validation host
        id: resolve_host
        env:
          SSH_HOST: ${{ inputs.ssh_host }}
        run: |
          if [[ -n "${SSH_HOST}" ]]; then
            echo "ssh_host=${SSH_HOST}" >> "${GITHUB_OUTPUT}"
            exit 0
          fi
          ssh_host="$(
            ANSIBLE_CONFIG=ansible/ansible.cfg \
              ansible-inventory -i ansible/inventory.ini --host "${{ inputs.target_host }}" \
              | python3 -c 'import json,sys; print(json.load(sys.stdin).get("ansible_host") or "")'
          )"
          if [[ -z "${ssh_host}" ]]; then
            echo "Failed to resolve ansible_host for target host" >&2
            exit 1
          fi
          echo "ssh_host=${ssh_host}" >> "${GITHUB_OUTPUT}"

      - name: Prepare validation known hosts
        env:
          SSH_HOST: ${{ steps.resolve_host.outputs.ssh_host }}
          SSH_PORT: ${{ inputs.ssh_port }}
          SSH_KNOWN_HOSTS: ${{ inputs.ssh_known_hosts }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          : > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
          if [[ -n "${SSH_KNOWN_HOSTS}" ]]; then
            printf '%s\n' "${SSH_KNOWN_HOSTS}" > ~/.ssh/known_hosts
            exit 0
          fi
          ssh-keyscan -p "${SSH_PORT}" -H "${SSH_HOST}" >> ~/.ssh/known_hosts

      - name: Validate k3s and gitops state
        env:
          SSH_HOST: ${{ steps.resolve_host.outputs.ssh_host }}
          SSH_USER: ${{ inputs.ssh_user }}
          SSH_PORT: ${{ inputs.ssh_port }}
          CHECK_NAME: ${{ matrix.check.name }}
          REMOTE_SCRIPT: ${{ matrix.check.script }}
        run: |
          printf '[validate:%s] running\n' "${CHECK_NAME}"
          {
            printf 'set -euo pipefail\n'
            printf 'export KUBECONFIG=/etc/rancher/k3s/k3s.yaml\n'
            printf '%s\n' "${REMOTE_SCRIPT}"
          } | ssh -i ~/.ssh/id_rsa \
            -p "${SSH_PORT}" \
            -o BatchMode=yes \
            -o UserKnownHostsFile="${HOME}/.ssh/known_hosts" \
            -o StrictHostKeyChecking=yes \
            "${SSH_USER}@${SSH_HOST}" \
            'bash -s'
