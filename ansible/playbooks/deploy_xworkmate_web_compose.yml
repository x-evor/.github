---
- name: Deploy xworkmate-web compose release
  hosts: xworkmate
  become: true
  vars_files:
    - ../vars/xworkmate-web.release.public.yml
  tasks:
    - name: Deploy xworkmate-web via shared compose role
      ansible.builtin.include_role:
        name: shared_compose_release
