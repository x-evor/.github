# Managed by Ansible. Do not edit on the host.
{% set merged_env = service_compose_env_common | combine(deploy_target.env | default({}), recursive=True) %}
{% for item in merged_env | dictsort %}
{% if item.1 is not none %}
{{ item.0 }}={{ item.1 }}
{% endif %}
{% endfor %}

